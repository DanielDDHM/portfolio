import { contactStyle } from './styles';
import { ScrollView, Text, View } from 'react-native';

export function Contact() {
  return (
    <View>
      <ScrollView>
        <Text>Contact</Text>
      </ScrollView>
    </View>
  );
}
