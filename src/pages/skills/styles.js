import { StyleSheet } from 'react-native';
import { default_colors } from '../../constants';

export const skills_style_mobile = StyleSheet.create({
  container: {
    borderColor: default_colors.secondary,
    borderWidth: 1,
    width: '100%',
    alignSelf: 'center',
    alignItems: 'center',
  },
  title: {
    color: default_colors.texts,
    fontSize: 30,
    textAlign: 'center',
    borderBottomColor: default_colors.secondary,
    borderWidth: 1,
  },
  div: { marginBottom: 10, flexDirection: 'row', flexWrap: 'wrap' },
});

export const skills_style_tablet = StyleSheet.create({
  container: {
    borderColor: default_colors.secondary,
    borderWidth: 1,
    width: '100%',
    alignSelf: 'center',
    alignItems: 'center',
  },
  title: {
    color: default_colors.texts,
    fontSize: 30,
    textAlign: 'center',
    borderBottomColor: default_colors.secondary,
    borderWidth: 1,
  },
  div: { marginBottom: 10, flexDirection: 'row', flexWrap: 'wrap' },
});

export const skills_style_laptop = StyleSheet.create({
  container: {
    borderColor: default_colors.secondary,
    borderWidth: 1,
    width: '50%',
    alignSelf: 'center',
    alignItems: 'center',
  },
  title: {
    color: default_colors.texts,
    fontSize: 30,
    textAlign: 'center',
    borderBottomColor: default_colors.secondary,
    borderWidth: 1,
  },
  div: { marginBottom: 10, flexDirection: 'row', flexWrap: 'wrap' },
});

export const skills_style_desktop = StyleSheet.create({
  container: {
    borderColor: default_colors.secondary,
    borderWidth: 1,
    width: '50%',
    alignSelf: 'center',
    alignItems: 'center',
  },
  title: {
    color: default_colors.texts,
    fontSize: 30,
    textAlign: 'center',
    borderBottomColor: default_colors.secondary,
    borderWidth: 1,
  },
  div: { marginBottom: 10, flexDirection: 'row', flexWrap: 'wrap' },
});

export const skills_style_large = StyleSheet.create({
  container: {
    borderColor: default_colors.secondary,
    borderWidth: 1,
    width: '50%',
    alignSelf: 'center',
    alignItems: 'center',
  },
  title: {
    color: default_colors.texts,
    fontSize: 30,
    textAlign: 'center',
    borderBottomColor: default_colors.secondary,
    borderWidth: 1,
  },
  div: { marginBottom: 10, flexDirection: 'row', flexWrap: 'wrap' },
});
